name: Nodejs-IPFS-Cloudflare

on:
#   push:
#     branches: [ master ]
#   pull_request:
#     branches: [ master ]
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [ 14.17.0 ]
        
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install -g ipfs-deploy --ignore-scripts
    - run: npm ci
    - run: npm run build --if-present
    - run: ipd _site -p pinata -d cloudflare
      env: 
        IPFS_DEPLOY_CLOUDFLARE__API_TOKEN=E9QOqnDxfTLVufwsZR5rglipJcEkcOxxo7vn03U1
        IPFS_DEPLOY_CLOUDFLARE__ZONE=jaack.me
        IPFS_DEPLOY_CLOUDFLARE__RECORD=_dnslink.ipfs.jaack.me
        IPFS_DEPLOY_PINATA__API_KEY=15aad1e3bb1cdd9aae7b
        IPFS_DEPLOY_PINATA__SECRET_API_KEY=6b7940d9f9998955fc2f96992a42edfcabec5c2b99359d66684ba471efbabac7
